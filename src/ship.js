const ship = (length) => {
  return {
    length,
  };
};

// class Ship {
//   array = [];
//   constructor(length) {
//     this.length = length;
//   }
//   hit(n) {
//     if(this.array.length === 0) {

//     }
//   }
// }

module.exports = ship;
